/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['assets.aceternity.com',' https://nextjs.org'], // Add your external image domain here
      },
};

export default nextConfig;
